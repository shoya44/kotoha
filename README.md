# ことは

PCの中で暮らしている話し相手です。同じPCの中に記憶を持ち、時々そちらから話しかけてきます。

依存は3つ（httpx / fastapi / uvicorn）。Windows で動きます。

## できること

| | |
|---|---|
| **話す** | ブラウザの会話画面から。外からは Tailscale 経由で同じ画面を開きます |
| **通話する** | 電話ボタンで、聞き取りと読み上げ。**言い終わった文から順に喋ります** |
| **覚える・忘れる** | 会話が途切れたころに、まとめて記憶へ。使われない記憶は期限で消えます |
| **思い出させる** | 「明日9時に歯医者って言って」と頼むと、その時刻に言います |
| **朝いちばんに教える** | 天気・傘・ゴミの日・今日の予定・寝ているあいだの地震を、1通にまとめて |
| **通知する** | 家に居なくてもスマホへ。**言ったことがそのまま通知**になります |
| **姿を出す** | タスクトレイに常駐すると、画面の右下に立ち絵が出ます |
| **PCの世話をする** | 様子を見ていて、聞かれたら答え、頼まれたら決められた操作だけします |

## 始め方

叩くのは **`kotoha.bat` の1枚だけ**です。あとはその後ろに言葉を付けます。

| 順 | やること |
|---|---|
| 1 | **`kotoha.bat setup`** ── Python環境と依存を用意します |
| 2 | **`kotoha.bat settings`** ── Geminiの鍵と合言葉を入れます |
| 3 | **`kotoha.bat tray`** ── タスクトレイに常駐します（ダブルクリックだけでも動きます） |

`kotoha.bat help` で、できることが並びます。

Gemini の鍵は [Google AI Studio](https://aistudio.google.com/) で取れます。通知を使うなら [OneSignal](https://onesignal.com/) の登録も要りますが、無くても動きます。

## 自分ぶんのファイル

**住んでいる場所や呼び名は、リポジトリに入っていません。** `data/`（git の外）に置きます。置かなければ、その話題が出ないだけです。

### 人格 ── `data/prompts/persona.txt`

`prompts/persona.txt` がひな形です（呼び名は「あなた」）。写して、名前や距離感を自分のものに直してください。置くと、そちらが優先されます。

> ⚠️ **ひな形のほうを直接書き換えないでください。** `prompts/` はリポジトリに入っているので、そこに書いた名前はそのまま公開されます。直すのは写したほう（`data/prompts/persona.txt`）です。

会話画面の「ことばを直す」も、**置いてあるほう**（効いている1枚）を書き換えます。

### ゴミの日 ── `data/calendars/garbage.json`

```json
{
  "area": "○○区 ××1〜3丁目",
  "source": "区のカレンダーのURL",
  "until": "2027-03-31",
  "weekly": {"0": "燃やすごみ", "1": "資源", "3": "燃やすごみ", "5": "プラスチック"},
  "monthly": {"4": ["燃やさないごみ", [2, 4]]},
  "breaks": [[[12, 24], [12, 31]], [[1, 1], [1, 10]]]
}
```

`weekly` は曜日（月曜=0）から品目へ。`monthly` は曜日から「品目と、出る週」へ（例は第2・第4金曜）。`breaks` は年末年始のように曜日どおりでない期間、`until` はそのカレンダーが使える最後の日です。**期限が切れると、ことはが朝に教えてくれます。**

### 仕事 ── `data/calendars/work.json`

```json
{
  "from": "10:00",
  "to": "18:00",
  "meetings": ["2027-04-16", "2027-05-21"]
}
```

祝日は公開情報なので `talk/schedule.py` に入っています。

## 画面

```
合言葉 → 会話画面 ──歯車→ 設定シート（1段だけ潜る）
                          記憶 / 頼まれごと / プロンプト / PCの様子 / 設定
```

- 送信欄の右が**電話ボタン**。押すと通話に入ります
- 返事の下に `Rm. 9/18 09:00 歯医者` と出たら、**その時刻で預かった**という印です
- 記憶は一覧から直せます。**大事なものはピン留め**すると消えません
- iPhone ではホーム画面に置くと、アプリのように開けます（通知もそこから）

## 設定

会話画面の歯車、`kotoha.bat settings`、`.env` の直接編集。どれも同じところを書きます。画面から変えたぶんは**すぐ効きます**。

とくに見ておきたいもの:

```
KOTOHA_WEB_TOKEN=            合言葉（空だと誰も入れません）
KOTOHA_LATITUDE=35.681       天気を見る場所（初期値は東京駅）
KOTOHA_LONGITUDE=139.767
KOTOHA_BRIEFING_HOUR=7       朝のひとことの時刻
KOTOHA_BACKUP_DIR=           控えのもう1本の置き場（空だとDBの隣だけ）
```

全項目は [.env.example](.env.example)、意味は [07 運用と設定](docs/07_運用と設定.md) にあります。

> ⚠️ **控えの置き場は設定しておいてください。** ことはの本体は記憶なので、DBの隣に1本しかないと、ディスクが1枚壊れた日にすべて消えます。

## 構成

```
kotoha/
  talk/         会話・人格・PCの様子・天気・地震・ゴミ・予定・暦の期限
  memory/       DB・記憶の整理と想起・ベクトル・頼まれごと
  serve/        Webサーバー・器のとりまとめ・ことはからの発言・裏の巡回
    static/     会話画面（素のHTML/CSS/JS）
  mascot/       デスクトップの姿（ctypes と自前のPNGデコーダ）
  tray.py       タスクトレイ常駐
  launcher.py   起動と配信
  config.py     設定の読み込みと検査
prompts/        固定ルール・人格のひな形・整理の指示
data/           DB・控え・自分ぶんのファイル（Git管理外）
docs/           資料
tests/          テスト（600件強・25秒ほど）
```

## 資料

[docs/](docs/) に、読む人ごとに分けて置いてあります。

- [コンセプト](docs/01_コンセプト.md) ── 思想・原則・語彙
- [機能](docs/02_機能.md) ── 何がどう動くか
- [処理フロー](docs/03_処理フロー.md) ── 分岐・取得元・送信先・プロンプト・返答加工
- [フロントエンド](docs/04_フロントエンド.md) / [バックエンド](docs/05_バックエンド.md) / [データベース](docs/06_データベース.md)
- [運用と設定](docs/07_運用と設定.md)
- [history/](docs/history/) ── 検討と見送り、事故の記録（現行仕様ではありません）

## テスト

```
python -m unittest discover -s tests -t .
```

外への通信は塞いであるので、本物のGeminiも通知も呼びません。push のたびに GitHub Actions でも同じものが走ります。

## 素材

立ち絵は自作です。再配布は自由にしてもらって構いません。
